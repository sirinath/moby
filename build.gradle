plugins {
	id 'leo.build-conventions'
}

description 'Leo Framework'

dependencies {
	testImplementation project(':testing')
}

wrapper {
	gradleVersion = '6.7'
	distributionType = Wrapper.DistributionType.ALL
}